.container.mt-4
	.col-md-6.mx-auto
		%h1 Registration
		%p Hi #{current_user.profile.first_name}!
		%p Thank you for your interest in registering for the #{@event.title} happening on [inside start time] until [insert end time].

		%p This event is free, so all you need to do is click register below.

		= form_with(model: [@event, @event_attendee]) do |f|
			= f.hidden_field :event_id, value: @event.id
			= f.hidden_field :user_id, value: current_user.id

			.row
				= f.submit "Register Now", class: "btn btn-lg btn-primary"