- if @channel.name == "ILY by Tricio"
	= render partial: "videos/ilybytricio", locals: {video: video}
- else
	.container-md.mb-4
		%video{src: video.original_upload.url, preload: "metadata", controls: "true", width: "100%"}
		%h1.mb-4 #{video.title}	
