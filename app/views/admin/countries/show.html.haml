= link_to "new State", new_admin_country_state_province_path(@country)

- @states.each do |s|
	.row.border.border-dark.pt-2.pb-2
		.col-2.text-center
			= s.unicode_flag
		.col-6
			= s.name
		.col-2
			= s.code
		.col-2
			= link_to "edit", edit_admin_state_province_path(s)