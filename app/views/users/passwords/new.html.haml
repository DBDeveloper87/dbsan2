.container.mt-4
  .col-md-4.mx-auto
    %h1.text-center Forgot your password?
    = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, data: { turbo: false } }) do |f|
      = render "devise/shared/error_messages", resource: resource
      
      %p Please enter your email address below, and if it exists in our system, we will email you a password reset link.

      .mb-4
        .input-group
          .input-group-text.bg-dark
            %i.bi.bi-envelope.text-light
          .form-floating.flex-grow-1
            = f.email_field :email, placeholder: "Email Address", class: "form-control border border-dark"
            = f.label :email, "Email Address"
            
      .row
        .col-md-6
          .row
            = f.submit "Reset My Password", class: "btn btn-primary btn-lg"
        .col-md-6
          .row
            = link_to "Back to Login",:back, class: "btn btn-secondary btn-lg"

