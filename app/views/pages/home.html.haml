.container.mb-4
  - if user_signed_in?
    %h1 Hello #{current_user.profile.first_name}
  - else
    %h1 Hello World
  
  - if user_signed_in?
    = button_to "Log out", destroy_user_session_path, method: :delete, data: { turbo: "false" }
    = button_to "Edit Account", edit_user_registration_path(current_user), method: :get

  %p <a class="btn btn-primary" data-bs-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">Link with href</a>
  .collapse#collapseExample
    .card.card-body
      Some placeholder content for the collapse component. This panel is hidden by default but revealed when the user activates the relevant trigger.
  